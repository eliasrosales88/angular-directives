import { BetterApproachDirective } from './better-approach.directive';

describe('BetterApproachDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterApproachDirective();
    expect(directive).toBeTruthy();
  });
});
